{
  "cron": [
    {
      "command": "bundle exec rake projects:sync",
      "schedule": "20 * * * *"
    },
    {
      "command": "bundle exec rake collections:sync",
      "schedule": "40 * * * *"
    }
  ]
}